export interface PackageJson {
  description: string;
  name: string;
  path: string;
  version: string;
}

export interface ProjectInfo {
  color?: string;
  description: string;
  environment?: string;
  icon?: string;
  name?: string;
  version?: string;
}
