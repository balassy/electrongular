export interface PackageJson {
  description: string;
  name: string;
  path: string;
  version: string;
}

export interface ProjectInfo {
  description: string;
  name: string;
  path: string;
  version: string;
}
